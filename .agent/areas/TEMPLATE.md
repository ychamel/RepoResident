# Area: <name>
<!-- Cap 60 lines. A future session reads this INSTEAD of re-deriving the module from code.
     Create one only when a session had to re-derive something non-obvious — that's the signal.
     Keep it true: maintain.md spot-checks these against the code. -->
Purpose: <one line>
Owner code: <directory/-ies>

## How it works (≤10 lines — the mechanism, not a file list)

## Key files
| Path | Role (one line) |
|---|---|

## Invariants (must stay true; breaking one is a bug even if tests pass)
-

## Gotchas (why the obvious approach fails here)
-

## Extending (the sanctioned pattern for the 2–3 most likely changes)
-
