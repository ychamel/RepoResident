# Board — who is working on what, in which branch. Lives on main; edited only via the claim and
# release steps in `workflows/integrate.md`. One row per in-flight branch, deleted at integration.
# Cap 20 rows. A claim is advisory: it warns, it does not enforce — overlap means talk first.

| Branch | Owner | Goal (one line) | Claimed areas (MAP paths) | Since |
|---|---|---|---|---|
| (none) | | | | |

## Integration log (last 10 merges; older lines deleted — full history is in git)
<!-- Format: `<YYYY-MM-DD> <branch> → main by <owner>: <one-line outcome>` -->
- (none)
