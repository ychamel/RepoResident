# <Project name — replaced at bootstrap>

This repository starts from an AI-agent harness template. The agent operating manual is
[CLAUDE.md](CLAUDE.md); the engine and the human manual live in [.agent/](.agent/README.md).

To begin, open an agent session and say:

> Bootstrap this: <one-paragraph project brief — what, stack, constraints>

Adopting an existing codebase instead? Copy `CLAUDE.md`, `AGENTS.md`, and `.agent/` into it, then:

> Bootstrap: adopt this repo.
